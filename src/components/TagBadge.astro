---
export interface Props {
  label: string;
  variant?: 'default' | 'accent' | 'grey';
}

const { label, variant = 'default' } = Astro.props;
---

<span class={`tag-badge tag-badge--${variant}`}>{label}</span>
